# 动作游戏、web 与状态机:转移是枚举的,还是计算的?

## 1. 为什么有人说动作游戏没有状态机

"动作游戏里没有状态机,用的是基于帧和取消标签的框架"——这个说法反对的不是状态机概念本身,而是**手工连线的显式 FSM**(如 Unity Animator 式的画格子连箭头)。

- **传统 FSM 的困境**:每个招式一个状态,任意两招可能互相取消,转移边数量呈 N² 爆炸,改一条取消规则要动无数连线,无法维护。
- **帧 + 取消标签框架**:系统只有一个泛化状态"正在播放某个动作"。每个动作是一份逐帧标注的数据(判定帧、无敌帧、取消窗口、派生规则)。运行时只有一条通用规则:输入缓冲中的指令类别,是否命中当前帧开放的取消标签,命中则切换动作。
- 结论:这仍是状态机,只是**数据驱动的隐式状态机**——状态 = 动作数据,转移 = 每帧求值的标签匹配规则。

## 2. 本质区别:转移是"枚举出来的"还是"计算出来的"

| 维度 | 传统 FSM | 帧标签 / 能力框架 |
|---|---|---|
| 转移的存在形式 | 显式的边,预先画在图里 | 不存在边,每次现场求值 |
| 复杂度归属 | 长在拓扑结构上,最坏 N² 条边 | 长在数据维度上(标签 × 条件),线性增长 |
| 条件粒度 | 状态级,条件多了导致状态组合爆炸 | 帧级 / 上下文级,可叠加运行时条件 |
| 修改语义 | 改结构(动图),程序员的事 | 改数据(改表 / 改配置),策划的事 |

比喻:FSM 像把所有城市间的航线一条条画在地图上;标签框架像只规定"跑道 ≥ X 的机场可起降 Y 类飞机",航线在查询时算出来。

## 3. 映射到支付系统

### 3.1 传统 FSM 写法(枚举边)

```java
Map<OrderStatus, Set<OrderStatus>> TRANSITIONS = Map.of(
    CREATED,          Set.of(PAYING, CLOSED),
    PAYING,           Set.of(PAID, CLOSED),
    PAID,             Set.of(PARTIAL_REFUNDED, REFUNDED),
    PARTIAL_REFUNDED, Set.of(PARTIAL_REFUNDED, REFUNDED)
);
```

问题:边只能表达"通 / 不通",表达不了"有条件地通"。"90 天内、风控未冻结、剩余金额 > 0"这类条件塞不进边里,只能造 `PAID_FROZEN`、`PAID_EXPIRED` 等新状态 → 状态数 = 原状态数 × 条件组合数,爆炸。(statechart 的 guard 正是针对这一步的解药,见第 5 节。)

### 3.2 能力求值写法(计算边)

每次请求到来时,对订单上下文**实时求值**当前具备哪些能力标签:

- 游戏的"帧" ≈ 订单的时间上下文(支付后第几天、支付窗口内外)
- "取消标签" ≈ 能力标签(REFUNDABLE / CLOSABLE / RETRYABLE)
- "输入缓冲" ≈ 进来的操作请求
- "标签命中则切换动作" ≈ 能力命中则执行操作

没有任何一处代码写着"PAID 可以转 PARTIAL_REFUNDED"——**边消失了**,合法性来自求值。改规则(90 天 → 180 天)只改一个条件,甚至可挪到配置中心。

### 3.3 混合架构:硬 FSM + 软能力层

纯能力模型的弱点:资金状态必须可枚举、可审计。真实系统分两层:

```
┌────────────────────────────────────────┐
│ 能力层(软):canPerform() 实时求值        │ ← 规则多、变化快、可配置
├────────────────────────────────────────┤
│ 资金层(硬):极小的严格 FSM + CAS         │ ← 极少变化、守不变量
│   INIT → FROZEN → SETTLED → REVERSED    │
└────────────────────────────────────────┘
```

分层标准:**违反它会丢钱 / 双花的规则进硬 FSM;违反它只是业务不允许的进能力层。**

游戏同构:"死亡后不能出招"是底层硬状态机(地面/空中/死亡)管的,十年不动;"轻攻击第 12 帧可接闪避"是标签层管的,策划天天改。

## 4. 关键领悟:能力定义的不是节点,而是线

- 传统 FSM 定义"节点能变到什么节点"(出边视角)。
- 能力模型定义"谁、在什么条件下能到达这个操作"(汇聚视角):pending→closed 和 paid→closed 两条边连同各自条件,收敛成一个可求值的 CLOSABLE。
- 条件本来长在边上,FSM 表达不了,硬塞进节点就状态爆炸;能力模型把条件从"状态的一部分"抽成"求值的一部分",状态得以保持粗粒度。
- 最终形态:**粗粒度大状态(守不变量、可审计)+ 能力求值(承载易变业务规则)**。

### 4.1 web 的接口面全是边

前端按钮和后端 controller 是同一件事的两端:GET 下发的"可用操作"= 当前状态的可走出边,**按钮就是边的渲染,点击就是走边**;API 路由表就是边的清单(`POST /invoice/:id/pay` 是一条边的名字)。这就是 HATEOAS 的本义——随资源返回的 links 就是出边。反过来看也成立:权限系统管的是"谁能走这条边"(guard),审计日志记的是"走过哪些边",测试用例清单就是边表遍历。**web 系统对外暴露的从来不是状态,是边。**

再精确半步:用户触发的其实是一条**宏边**——从稳定状态到稳定状态的一个操作,内部可能推进多次状态(占位 → 调第三方 → 落定)。系统里因此有两张图:**操作图(宏边)**给用户、产品、API 文档看,**转移图(微边)**给审计、对账、状态机自己用——前者是后者的商图,"归一"就是那个商映射。

### 4.2 归一的判据:按语义,不按落点

一个状态有多条入边到达,这些边**有条件地**可以归一成一个操作。判据是**语义 + 准入条件同源**,而不是落点相同:

- **多个操作 → 同一落点**:超时关单 / 用户取消 / 风控关闭都到 CLOSED——按落点归一会把三个语义不同的操作错误合并(各自的条件、权限、审计含义都不同)。
- **一个操作 → 多个落点**:支付可能落到"处理中",也可能被风控退回原状态——按落点拆分又会把一个操作劈开。

操作与落点是**多对多**的:操作的同一性由"语义(业务动词)+ 准入条件"定义,落点只是实现细节。这是 6.3 节"问操作、不问目标状态"的另一面——入口不以目标状态为键,归一也不以目标状态为键。安全的归一只有一个方向:先确认语义同一,再看边表是否可合并;落点相同只是伴随现象,不是理由。

### 4.3 边界:接口面是边,不变量面是状态

"边比状态重要"要说精确。对外的一切(按钮、路由、权限、文档)确实都是边;但动词表之所以能保持这么小,是因为名词账本在底下守着——"钱的状态必须可枚举""死亡后不能出招"这类铁律长在**状态**上,不长在任何一条边上。收束成一句:**状态是系统对内记账的名词,边是系统对外暴露的动词;拉模式的系统没有自发演化,它的全部动态就是边表——入边按语义归一之后,边表就是 API。**

## 5. 中途站:statechart —— 边仍是枚举的,条件已是计算的

写完前几节后发现,这条问题线早有人走过一半:Harel 1987 年的 statechart(《Statecharts: A Visual Formalism for Complex Systems》,现代实现如 XState / Stately)。[Stately 文档](https://stately.ai/docs/state-machines-and-statecharts#what-is-a-statechart)的概括:

> "Statecharts add extra features not available in ordinary state machines, including hierarchy, concurrency and communication."

这三个特性,加上 statechart 同样具备的 guard(守卫条件),恰好逐条对应本文前几节的痛点:

| statechart 特性 | 对应的本文痛点 | 怎么解 |
|---|---|---|
| **guard**(守卫条件) | 边只能表达"通 / 不通"(3.1 节) | 条件直接挂在边上:`PAID --REFUND [90 天内 && 未冻结]--> REFUNDED`,不必造 PAID_FROZEN |
| **hierarchy**(层级状态) | 转移边 N² 爆炸(第 1 节) | 共享转移上收到父状态,一条边顶一族边;地面 / 空中 / 死亡就是父状态,同构于 3.3 节的"硬 FSM + 软层"分层 |
| **concurrency**(并行区域) | 状态数 = 原状态 × 条件组合(3.1 节) | 资金 × 风控 × 履约本是正交维度,普通 FSM 被迫取笛卡尔积,statechart 各画各的区域 |
| **communication**(actor 通信) | 多来源请求竞争同一订单(第 7 节) | 支付回调与超时机器人就是两个 actor,各自向订单机器发消息 |

所以光谱上其实有三档,statechart 是中间站:

```
纯 FSM              statechart                能力模型
枚举边、无条件    →   枚举边 + 计算的条件(guard)  →   计算的准入,边只作为元数据声明
```

statechart 把条件从节点里救了出来(guard 让"有条件地通"终于可以表达),但**边本身仍是枚举的**——guard 只是给画好的边加门,图仍然驱动运行。能力模型多走的那半步,是连边也不画了,准入现场求值。

为什么 statechart 停在中间?用第 7 节的推 / 拉之分回看就清楚了:Harel 当年是给战斗机航电设计的——典型的**推模式**反应式系统,多个并行区域每一刻同时演化,必须保留完整的出边图做仲裁,图拆不得。而拉模式的 web 业务没有"下一帧",不需要出边图,才有条件把边的逻辑拆进求值。工具形态也印证这一点:Stately Studio 的做法是让**图就是代码**(可视化编辑,图直接驱动逻辑),用"图即真相"防止图腐烂;第 10 节走的是反方向——图是从元数据生成的视图。两条路解决的是同一个问题:图与逻辑的漂移。

由此得到的不是二选一,而是一条升级阶梯:

1. 状态少、边上无条件 → 邻接白名单就够(纯 FSM);
2. 边上长出条件 → guard / statechart;
3. 同一落点被多个语义不同的操作汇聚、条件多到要进配置中心 → 转置为能力模型,边退化成元数据声明。

触发升级的信号始终是同一个:**当你想造 PAID_FROZEN 这种"状态 × 条件"的组合状态时,说明当前档位到头了。**

## 6. 工程落地设计

这一节最初写于知道 statechart 之前。按第 5 节的阶梯重写:第二档和第三档的落地长得不一样,动手前先判断自己在哪一档。

### 6.1 第二档(statechart):机器定义就是注册表

停在第二档时,**不需要手写能力注册表**——结构前提(什么状态下有这条边)就是机器里枚举的边,业务条件就是边上的 guard,两者都内建在机器定义里,`snapshot.can(event)` 一步求值。canPerform 退化成一层薄壳,只负责补上拒绝原因:

```ts
// 规则单源:同一份谓词,machine 的 guard 用它,拒绝原因也用它
const RULES = {
  notExpired:       (ctx, now) => now < ctx.expiresAt,
  refundWindowOpen: (ctx, now) => ctx.paidAt !== null && now - ctx.paidAt <= REFUND_WINDOW,
}

function canPerform(snap, action, now): CheckResult {
  if (snap.can({ type: action, now })) return { ok: true }
  return { ok: false, reason: firstFailingRule(action, snap.context, now) }
}
// 全量求值,给前端亮按钮
const evaluateAll = (snap, now) => ACTIONS.filter(a => canPerform(snap, a, now).ok)
```

两个要点:

- `now` 不是在 guard 里隐式读时钟,而是随事件显式传入(`{ type: 'PAY', now }`),guard 从 `event.now` 取——求值仍是纯函数,可测试、可回放。
- 第 10 节手工声明的 `allowedFrom / possibleTo` 元数据在这一档也退场了:机器定义本身就是元数据,状态图直接由它生成(Stately 的可视化就是这么来的)。

完整可运行示例(XState + 模拟数据库 CAS + 超时机器人):[`playground/play-xstate.ts`](playground/play-xstate.ts)。

### 6.2 第三档(能力模型):注册表 + 按需求值(不用 static 方法)

爬到第三档、边不复存在时,机器定义没法再充当注册表,才需要手写一个。static 方法拿不到注入依赖(riskService 等)、无法 mock、易散落。改为:

```java
@Component
public class CapabilityEvaluator {
    private final Map<OrderAction, BiPredicate<Order, Instant>> rules; // 构造器里注册

    public CheckResult canPerform(Order order, OrderAction action, Instant now); // 单点、按需
    public Set<OrderAction> evaluateAll(Order order, Instant now);               // 全量,给前端亮按钮
}
```

- 操作校验走 `canPerform`(惰性,只付一个能力的代价);展示场景走 `evaluateAll`。
- 规则内部条件排序:便宜的本地判断在前,贵的 RPC 在后,靠 `&&` 短路省远程调用。
- 求值保持**纯函数**:`now` 显式传入(可测试),不做写操作,可返回拒绝原因(CheckResult)。

### 6.3 统一入口:问"操作",不问"目标状态"

签名:`canPerform(Order order, OrderAction action, Instant now)`,而非 `canChangeTo(order, targetStatus)`。理由:

1. 同一目标状态对应多个语义不同的操作(超时关单 / 用户取消 / 风控关闭,条件各异)。
2. 有些操作不改状态(重试支付、部分退款自环)。
3. 目标状态是实现细节,操作才是业务语言;状态设计重构不影响调用点。

操作 → 状态的映射放在执行器内部,校验与执行分离。游戏同理:输入缓冲存的是"玩家按了闪避"这个指令,不是"想切到闪避动画第 0 帧"。

statechart 里这条原则是**自动成立**的:`send(event)` 发的就是操作,落点由机器内部的边决定,调用方根本没有指定目标状态的入口。这也是它比"调用方直接 `transition(target)`"的裸 FSM 更进一步的地方。

## 7. 更深一层:推模式 vs 拉模式,仲裁 vs 准入

- **动作游戏 = 推模式**:系统每帧主动演化,多个候选转移竞争(受击 / 闪避 / 攻击同时在场),核心难点是**仲裁**——优先级、输入缓冲、精确到 1/60 秒的帧窗口。查询方向:`当前状态 → ?`
- **web 业务 = 拉模式**:订单不被碰就不变,每次变化由一个明确请求驱动、一次一个意图,核心难点是**准入**——条件校验、幂等、并发防重。查询方向:`? → 目标操作`
- 抽象结论:**主动演化的系统关心"我接下来会变成什么"(出边 + 仲裁);被动响应的系统关心"外界能对我做什么"(入边 + 准入)。** 支付系统属于后者——没有"下一帧",只有"下一个请求"。这是能力模型在 web 业务里更顺手的根本原因。
- 补充:连超时关单也是拉——定时任务只是"代替人按按钮的机器人",到点仍走一遍准入检查。web 处理并发竞争不靠优先级仲裁,靠**数据库锁 + CAS 让先到者赢**(可接受拒绝后重试;游戏不行,丢一帧输入手感就毁)。

## 8. 超时关单的实现与规则漂移陷阱

两种做法:

1. **捞候选 → 逐单 canPerform → 关单**:规则单源,量大时慢。
2. **SQL 直接批量关**:快,但 WHERE 子句是把 CLOSE 规则用 SQL 复述了一遍 → 规则存在两份,改 Java 忘 SQL 就出现"页面显示不可关、定时任务却关了"的**规则漂移**。这是能力模型最常见的腐化路径:单一事实来源丢失。

推荐的完整链路:

```
粗筛(SQL:只用 status + 时间等稳定条件,索引友好,宁多捞不漏捞)
  → 细筛(canPerform:完整规则,权威版本永远只在 evaluator 里)
  → 执行(CAS:UPDATE ... WHERE id=? AND status='CREATED',affected=0 即状态已变,安静退出)
```

CAS 一步不可省:粗筛到执行有时间差,支付回调与超时关单竞争同一订单,谁先 CAS 成功谁赢。

## 9. 存储状态 ≠ 展示状态:"过期"的判断权与记录权

选了"expired 不落成状态"(见 [`playground/play-xstate.ts`](playground/play-xstate.ts) 的 invoice 机器:pending / processing / paid / closed,唯独没有 expired),就会踩到这套模型必然出现的最后一块拼图:**存储状态(state)和展示状态(display status)从此不是同一个东西**。机器里的 state 是"事实"(pending),"过期"是"事实 + 当前时刻"的派生结论——而页面要显示的从来都是派生结论。

### 9.1 展示层:displayStatus = f(state, context, now)

派生函数必须同时吃 state 和求值结果,因为同一个 `now ≥ expiresAt` 在不同 state 下含义不同:

- state = pending → 显示"已过期"
- state = processing → 显示"支付处理中"(在途的支付不能因为过了截止线就判死,要等终态回来)
- state = paid → 显示"已支付"(过期截止时刻早就无关了)

processing 这条尤其重要——不能图省事写成"先算 expired,是就直接显示过期",求值结果只在特定 state 下才有意义。guard 挂在 pending 的 PAY 上而不是全局,和显示逻辑只在 pending 下认过期,是**同一条规则的两个投影**。

腐化路径可预见:订单列表页写一遍 `status === 'pending' && now > expiresAt`,详情页再写一遍,小程序端又写一遍——这就是第 8 节规则漂移的展示层版本。解法同样是单一事实来源:收敛成一个 selector,所有端、所有页面只准调这一个函数;后端出 API 时最好直接把 displayStatus 算好下发,把派生逻辑收敛到离事实最近的地方。(实现细节:列表页批量渲染时 `now` 在一次渲染里取同一个值,否则临界时刻同屏会出现有的行过期、有的没过期。)

顺带说破:即使把 expired 物化存了,这个分层也照样存在——PARTIAL_REFUNDED 存储上是一个状态,页面上可能要按退款明细显示"退款中 / 已退部分 / 退款失败"。物化只是让"过期"这一个 case 的派生变成透传,没有消灭这一层。

至此,完整的分层图景:

```
存储状态(极小 statechart)        → 事实,给审计、对账、状态机自己用
能力层 canPerform(fact, now)     → 操作准入、按钮亮灭
展示层 displayStatus(fact, now)  → 给人看的那个"状态"
```

三者都从同一份事实派生,规则各自单源。用户看到的"已过期"和数据库里躺着的 pending 并不矛盾——前者是视图,后者是事实,和第 10 节"图是从元数据生成的视图"是同一件事:**展示状态也是生成物,不是存储物**。

### 9.2 物化的延迟不可消除,出错与否看谁消费

反过来把 expired 落成状态存下来会怎样?`expiresAt = 14:30:00` 是一个精确时刻,但把它落成状态的动作永远是异步的:定时任务有扫描间隔,消息会积压,任务会挂掉重启。所以物化后的 status 字段有一个永恒性质:

> 存在一个窗口,里面躺着"逻辑上已过期、字段上还是 pending"的脏数据。窗口可以压小(扫描更频繁、用延迟消息),永远压不到零。

延迟本身不可消除;它是否造成"判断出错",取决于谁在消费这个字段:

- **拿它做准入 → 必错。** 支付入口写 `if (status == PENDING)` 放行,那么 14:30:00 到扫描落地之间的请求全部被错误放行——过期发票被支付了。这个 bug 特别阴险:平时扫描间隔 5 秒没人撞上,某天任务积压 10 分钟,一批"不可能发生"的订单就进来了。
- **拿它做展示 / 报表 → 可容忍。** 列表页晚 60 秒显示"已过期"通常没人在意,按天聚合的报表更无所谓。

关键认知转变在这:一旦物化,status 字段就从"事实"降格成"物化视图 / 缓存",而**缓存的第一定律是可能过期**(一语双关)。真正的事实是 `(状态, expiresAt, now)` 这组数据,expired 永远是从它派生的。

但要防住反方向的误解:"物化有延迟"不等于"物化是错的"。求值也有自己的失效方式——规则漂移(SQL 里复述一遍谓词)、各端各自实现、时钟不同步。物化的风险在数据滞后,求值的风险在规则多份,没有免费的方案。而且写入点的最终防线不靠这两者,靠数据库层面的原子条件:

```sql
UPDATE invoice SET status = 'PROCESSING'
 WHERE id = ? AND status = 'PENDING' AND expires_at > NOW()
```

affected = 0 就拒绝。即使走求值,`now < expiresAt` 判断通过后到落库之间也有微小窗口(以及并发的支付回调),所以:**求值是第一道闸(精确、无延迟,挡住绝大多数),CAS 是最后一道闸(挡住窗口里漏网的)。**

收束成一句判据:**过期的"判断权"永远归求值,"记录权"可以给物化;物化字段一旦被拿去行使判断权,就是 bug。**

游戏里的对应又出现了:帧窗口开没开永远是当帧现场算的,没有哪个格斗游戏会提前一帧把"取消窗口已开"写进某个变量、下一帧再去读——读到的必然是上一帧的旧真相。**状态可以缓存,时间窗口的判定不能缓存。**

## 10. 如何"看见"能力 / 动作带来的状态变化

能力模型的代价:边被拆成"准入条件"(evaluator)和"状态落点"(executor)两半后,**没有任何一处能直接看到完整的转移图**。FSM 的图既是逻辑又是文档,拆掉逻辑的同时文档也没了。

解法不是把边逻辑加回来,而是**把边作为"声明"加回来**——图不再驱动代码,而是由元数据生成。分三层:

### 10.1 在 Action 上声明结构性元数据(设计期可见)

```java
public enum OrderAction {
    PAY       (Set.of(CREATED),                Set.of(PAYING)),
    CLOSE     (Set.of(CREATED, PAID),          Set.of(CLOSED)),
    REFUND    (Set.of(PAID, PARTIAL_REFUNDED), Set.of(PARTIAL_REFUNDED, REFUNDED)),
    RETRY_PAY (Set.of(PAYING),                 Set.of(PAYING));  // 自环

    final Set<OrderStatus> allowedFrom;  // 结构性前提:什么状态下该动作才有意义
    final Set<OrderStatus> possibleTo;   // 可能落到哪些状态
}
```

分工:`allowedFrom / possibleTo` 只声明**图的形状**(粗粒度结构前提);90 天窗口、风控等**业务条件**仍留在 evaluator。`canPerform` 第一步先查 `allowedFrom`,不满足直接拒绝,再走业务规则。

### 10.2 执行侧收口到单一提交点(运行期可控)

状态变更不允许散落各处 `setStatus`,全部经过一个 choke point:

```java
void commitTransition(Order order, OrderAction action, OrderStatus to) {
    if (!action.possibleTo.contains(to)) {
        // 执行器想落到声明之外的状态 → 代码有 bug 或声明过期,fail fast
        throw new UndeclaredTransitionException(action, order.getStatus(), to);
    }
    transitionLog.append(order.getId(), order.getStatus(), to, action, operator, now);
    casUpdate(order, to);  // WHERE id=? AND status=?
}
```

这一步同时防止"声明与实现漂移":executor 哪天写出声明外的落点,立刻炸出来,而不是悄悄漂移。

### 10.3 图从元数据生成,事实从日志回放(两种"看见")

- **设计期看见**:小工具遍历 `OrderAction` 输出 Mermaid / DOT,自动生成状态图进文档。图永远与代码同步——因为图就是从代码生成的,边上可标注 action 名与条件摘要。
- **运行期看见**:`transition_log` 记录每笔订单的 (from, to, action, operator, time, reason)。"这单怎么变成 CLOSED 的"查日志而不是猜;还可对账"日志中出现过的转移 ⊆ 声明的图"来验证系统行为。

| | 传统 FSM | 能力模型 + 声明 |
|---|---|---|
| 图的角色 | 图**是**逻辑,驱动运行 | 图是从元数据生成的**视图** |
| 图与代码同步 | 靠人维护,会烂 | 自动生成,不会烂 |
| 实际发生了什么 | 假设等于图 | 日志记录事实,可对账 |

游戏同构:策划工具从动作数据表反向生成派生树 / 取消关系图供人审阅(格斗游戏 frame data 网站本质就是这个),运行时靠回放与帧日志排查——没人靠手画的状态图理解连段系统,图都是从数据渲染出来的。

核心口诀:**边的逻辑留在求值里,边的形状声明在元数据里,边的事实记录在日志里**——三者各司其职,图变成永不过期的生成物。

## 11. 总结

用最小的显式状态机守住不变量(钱的状态、角色的生死),用集中注册、按需求值、以操作为入口的能力规则承载所有易变的业务策略(能不能退、能不能取消、第几帧能不能取消)——转移不再被枚举,而是被计算;图不再被维护,而是被生成;给人看的"状态"不再被存储,而是被派生。

这条路 statechart 在 1987 年已经走到中途:hierarchy、concurrency、guard 都是对同一场状态爆炸的回答,只是它为推模式的反应式系统而生,边必须留在图里。能力模型走完的是拉模式下的最后半步——边从驱动逻辑的图里退休,以元数据声明的身份留任。